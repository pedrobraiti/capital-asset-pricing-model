# Normalize line endings: LF in the repository and on checkout.
* text=auto eol=lf

# Treat these as binary (no EOL normalization / diff noise).
*.png binary
*.pdf binary
*.parquet binary
*.pkl binary
